# data/legacy

This folder stores historical metadata files that are still useful for thesis reproduction but are not the primary inputs for the refactored workflows.

Current contents:
- `aptos2019_labels.csv` - full APTOS-style `id_code,diagnosis` labels without split assignment
- `messidor_data.csv` - Messidor metadata with DME and gradability columns

Do not place generated outputs here.
