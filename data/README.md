# data

This folder stores tracked input metadata such as `dataset.csv`, `train.csv`, `valid.csv`, and `test.csv`.

Do not write generated checkpoints, figures, or evaluation outputs here. Generated files belong under `artifacts/`.
